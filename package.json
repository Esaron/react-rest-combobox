{
  "name": "esaron-react-rest-combobox",
  "version": "4.0.1",
  "description": "Simple combobox-like component that will allow selection of formatted options from a rest endpoint.",
  "main": "dist/js/react-rest-combobox.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-dev": "babel src --out-dir dist --copy-files",
    "build-min": "npm run build-dev && uglifyjs -c -o ./dist/js/react-rest-combobox.min.js ./dist/js/react-rest-combobox.js",
    "prepublish": "npm run build-min"
  },
  "author": "esaron",
  "license": "UNLICENSED",
  "dependencies": {
    "esaron-react-link": "1.0.0",
    "jquery": "^3.2.1",
    "react": "^15.1.0",
    "react-bootstrap": "^0.30.0",
    "react-dom": "^15.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.11.1",
    "babel-preset-stage-2": "^6.11.0",
    "uglify": "^0.1.5"
  }
}
